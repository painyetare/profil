import "../globals.css";
import Cursor from '@/components/cursor/Index';

export const metadata = {
  title: "painydev | Mind the business, we build the product",
  description: "Made with love",
};

export default function RomanianLayout({ children }) {
  return children;
}